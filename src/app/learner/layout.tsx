import "@/styles/legacy-portal.css";
import "@/styles/legacy-settings.css";
import "@/styles/theme-refined.css";

export default function LearnerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
